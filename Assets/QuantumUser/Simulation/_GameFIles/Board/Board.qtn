component Board
{
    entity_ref Ref;
    PlayerLink Player1;
    PlayerLink Player2;
    list<Hero> HeroesID1;
    list<Hero> HeroesID2;

    // Fighing phase only
    list<FightingHero> FightingHeroesMap;
    list<HeroProjectile> HeroProjectiles;
}

struct FightingHero
{
    Hero Hero;
    int Index;
    int BoardIndex;
}