component Board
{
    entity_ref Ref;
    PlayerLink Player1;
    PlayerLink Player2;
    list<HeroEntity> HeroesID1;
    list<HeroEntity> HeroesID2;

    // Fighing phase only
    list<FightingHero> FightingHeroesMap;
    list<HeroProjectile> HeroProjectiles;
    list<GlobalEffectQnt> GlobalEffects;
}