component Board
{
    entity_ref Ref;
    PlayerLink Player1;
    PlayerLink Player2;
    list<HeroEntity> Heroes1;
    list<HeroEntity> Heroes2;

    // Fighing phase only
    list<FightingHero> FightingHeroesMap;
    list<HeroProjectile> HeroProjectiles;
    list<GlobalEffectQnt> GlobalEffects;
}