struct Hero
{
    entity_ref Ref;
    Int32 ID;
    Int32 Level;
    FPVector3 DefaultPosition;

    // Stats
    FP Health;
    FP Defense;
    FP Damage;
    FP AttackSpeed;
    FP ProjectileSpeed;
    int Range;
    FP RangePercentage;

    // Fighing stats only
    FP CurrentHealth;
    entity_ref AttackTarget;
    int TargetPositionX;
    int TargetPositionY;
    int TeamNumber;
    bool IsAlive;
    FP AttackTimer;
}